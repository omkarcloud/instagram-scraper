services:
  instagram-scraper:
    build: .
    restart: unless-stopped
    ports:
      - "8000:8000"
    environment:
      - PORT=8000
      # Optional — Instagram does not rate-limit at normal volume, so leave this unset.
      # - INSTAGRAM_PROXY=http://user:pass@host:port
